# Copy to application.properties for local overrides if needed.
# Prefer environment variables / application.yml defaults.

spring.application.name=mis-backend
spring.datasource.url=jdbc:mysql://localhost:3306/university_mis?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
server.port=8080
app.jwt.secret=UniversityMisJwtSecretKeyChangeMeInProduction2026!!
app.cors.origins=http://localhost:3000,http://localhost:5173
